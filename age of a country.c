
#include <stdio.h>

int main() {
    int currentYear, age;

    // Taking input
    scanf("%d", &currentYear);

    // Calculating age
    age = currentYear - 1971;

    // Output the result
    printf("%d\n", age);

    return 0;
}
